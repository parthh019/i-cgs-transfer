{
  "name": "certifypro-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix frontend",
    "dev": "node scripts/dev.mjs",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "node scripts/dev.mjs backend",
    "build": "npm run build --prefix frontend"
  }
}
